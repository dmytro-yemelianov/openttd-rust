[package]
name = "transport-sim"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
transport-types = { path = "../transport-types" }
transport-world = { path = "../transport-world" }
thiserror = "1.0"
tracing = { version = "0.1"}

[[bench]]
name = "sim_benchmark"
harness = false